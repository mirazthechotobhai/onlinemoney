rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Remote sessions for pairing remote controller with main screen
    match /remote_sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
